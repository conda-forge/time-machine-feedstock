# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "3.0.0"

package:
  name: time-machine
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/t/time-machine/time_machine-${{ version }}.tar.gz
    sha256: c75b5d91f711d10ff4052f00e6cf6869b2de358d1fc2ec224ba22ac629b17638
    target_directory: dist
  - url: https://github.com/adamchainz/time-machine/archive/refs/tags/${{ version }}.tar.gz
    sha256: 5e1f582f91f9ebc9161a8d961c75f9f64ec089110b3e466a4715b5c33232c68a
    target_directory: src

build:
  number: 0
  script:
    - cd dist
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  build:
    - ${{ compiler("c") }}
    - ${{ stdlib("c") }}
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
  host:
    - pip
    - python
    - setuptools
  run:
    - python
    - python-dateutil

tests:
  - python:
      imports: time_machine
      pip_check: true
  - if: osx or linux64 or (linux and match(python, ">=3.12"))
    then:
      files:
        source:
          - src/tests/
      requirements:
        run:
          - backports.zoneinfo
          - coverage
          - pytest
          - pytest-randomly
          - python-dateutil
          - tokenize-rt
      script:
        - coverage run --source=time_machine --branch -m pytest src/tests -vv --color=yes --tb=long -p pytester
        - coverage report --show-missing --skip-covered --fail-under=98

about:
  summary: Travel through time in your tests.
  license: MIT
  license_file: dist/LICENSE
  homepage: https://github.com/adamchainz/time-machine

extra:
  recipe-maintainers:
    - bollwyvl
